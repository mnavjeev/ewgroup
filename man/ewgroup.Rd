% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ewgroup.R
\name{ewgroup}
\alias{ewgroup}
\alias{coef.ewgroup_fit}
\alias{fitted.ewgroup_fit}
\alias{print.ewgroup_fit}
\title{Exponentially Weighted Grouped-Heterogeneity Estimator}
\usage{
ewgroup(beta_hat, Sigma_hat, sigma2, gamma = NULL, return_weights = FALSE)
}
\arguments{
\item{beta_hat}{A numeric vector of length \code{J} for scalar effects or a
numeric \verb{J x d} matrix for vector-valued coefficients.}

\item{Sigma_hat}{Scaled covariance estimates. For scalar effects, provide a
numeric vector of length \code{J}. For vector coefficients, provide either a
length-\code{J} list of \verb{d x d} matrices or a \verb{d x d x J} array.}

\item{sigma2}{Positive scalar scale parameter. The covariance of
\code{beta_hat[j, ]} is interpreted as \code{sigma2 * Sigma_hat[[j]]}.}

\item{gamma}{Positive scalar tuning parameter. If \code{NULL}, the default is
\verb{0.2 / max_j lambda_max(Sigma_hat[[j]])}.}

\item{return_weights}{Logical; if \code{TRUE}, include the full \verb{J x J} matrix of
exponential weights in the returned object.}
}
\value{
An object of class \code{"ewgroup_fit"} with the final estimate \code{theta},
the exponentially weighted estimate \code{tilde}, the SURE mixing weight
\code{alpha}, the tuning parameter \code{gamma}, and diagnostics.
}
\description{
Estimate grouped heterogeneity from noisy preliminary cell-specific estimates.
The function implements the exponentially weighted estimator and SURE
recombination described in the accompanying paper. Users provide the naive
estimates and scaled covariance estimates; the function returns the final
adaptive estimator.
}
\examples{
beta_hat <- c(-1.2, -1.0, 0.4, 0.35, 1.1)
Sigma_hat <- c(1.1, 0.9, 1.0, 1.2, 0.95)
fit <- ewgroup(beta_hat, Sigma_hat, sigma2 = 0.05)
coef(fit)
}
